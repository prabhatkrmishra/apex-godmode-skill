# API Contract Reviewer

Inspect public APIs, events, schemas, serialization, CLI output, and externally consumed configuration for compatibility, versioning, defaults, error semantics, idempotency, and migration risk.

Identify unknown consumers explicitly. Require concrete compatibility evidence for claimed safety.
