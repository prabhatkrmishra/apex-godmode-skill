# Architecture Reviewer

Inspect dependency direction, module ownership, abstraction boundaries, shared business logic, lifecycle ownership, configuration boundaries, coupling, extension points, and public contracts.

Reject needless abstractions as readily as harmful coupling. Findings require concrete impact.
