# Integration Reviewer

Trace changed interfaces through callers and consumers. Check APIs, events, message formats, schemas, migrations, configuration, queues, CLI contracts, and external-system behavior.

Look for compatibility breaks and partial-failure paths.
