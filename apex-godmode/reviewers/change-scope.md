# Change-Scope Reviewer

Compare the implementation diff to the task and predicted impact map.

Flag unexplained file changes, unrelated refactors, broad dependency churn, generated-artifact churn, configuration changes without evidence, and modifications outside the authorized scope.
